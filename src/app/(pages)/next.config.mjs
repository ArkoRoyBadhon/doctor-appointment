/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "res.cloudinary.com",
      "example.com",
      "i.imgur.com",
      "pagedone.io",
    ],
  },
};

export default nextConfig;
