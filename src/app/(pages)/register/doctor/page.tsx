import Controller from '@/components/doctorSignup/Controller';
import React from 'react';

const page = () => {
    return (
        <main>
            <Controller />
        </main>
    );
};

export default page;