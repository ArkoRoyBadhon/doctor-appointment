import EditControl from '@/components/profile/EditControl';
import React from 'react';

const page = () => {
    return (
        <div>
            <EditControl />
        </div>
    );
};

export default page;